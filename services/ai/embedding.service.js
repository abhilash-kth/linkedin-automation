// Placeholder for embeddings — skip for now, add when needed
// Can use OpenAI text-embedding-3-small or local sentence-transformers

export async function generateEmbedding(text) {
  // TODO: Integrate when vector DB is set up
  console.log(`   ⚠️  Embedding service not configured yet`);
  return null;
}